// Function that updates the HTML content
function revealMessage() {
    // Assigning two variables
    var fact = "Welcome to the cosmos! ";                                               // Creating the Variable
        fact += "Did you know that one million Earths could fit inside the Sun?";       // Using += operator to add to the variable
        document.getElementById("CosmicText").innerHTML = fact;                         // Utilizing document.getElementById to change the text

}